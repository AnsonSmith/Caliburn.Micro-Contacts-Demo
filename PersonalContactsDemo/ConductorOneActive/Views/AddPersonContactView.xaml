<UserControl x:Class="ConductorOneActive.Views.AddPersonContactView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">

    <StackPanel>
        <Button x:Name="CloseGame"  HorizontalAlignment="Right"/>
        <TextBlock Text="Title:" />
        <TextBox x:Name="Title">
            <TextBox.CaretBrush>
                <SolidColorBrush Color="Gray" />
            </TextBox.CaretBrush>
        </TextBox>

        <TextBlock Text="Rating:" />
        

        <TextBlock Text="Notes:" />
        <TextBox x:Name="Notes" Height="120">
            <TextBox.CaretBrush>
                <SolidColorBrush Color="Gray" />
            </TextBox.CaretBrush>
        </TextBox>

        <Button x:Name="AddGame" Content="Add" HorizontalAlignment="Right" Margin="0 12 0 0" />
    </StackPanel>
</UserControl>
